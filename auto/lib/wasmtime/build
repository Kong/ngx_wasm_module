if [ ! -x "$(command -v cargo)" ]; then
    echo "$0: error: $ngx_addon_name could not build the \"$NGX_WASM_RUNTIME\" runtime."
    echo "Missing command: cargo"
    echo
    exit 1
fi

cargo build --release -p wasmtime-c-api

# vim:ft=sh ts=4 sts=4 sw=4:
