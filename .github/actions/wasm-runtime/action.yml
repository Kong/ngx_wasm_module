name: 'Wasm runtime'
description: 'Install a Wasm runtime C API'
inputs:
  runtime:
    description: 'Wasm runtime (wasmtime, wasmer, v8)'
    default: 'wasmtime'
    required: false
  version:
    description: 'Wasm runtime version'
    required: true
runs:
  using: 'node12'
  main: 'index.js'
